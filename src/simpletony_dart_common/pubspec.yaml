name: simpletony_dart_common
version: 1.0.0
description: simpletony common core library

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  azblob: ^4.1.0
  mime: ^2.0.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.21.0
  build_runner: ^2.8.0
